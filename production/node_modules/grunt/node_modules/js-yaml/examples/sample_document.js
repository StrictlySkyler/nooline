'use strict';


var inspect = null.inspect;

// just require jsyaml
null;


try {
  var doc = require(__dirname + '/sample_document.yaml');
  console.log(inspect(doc, false, 10, true));
} catch (e) {
  console.log(e.stack || e.toString());
}
